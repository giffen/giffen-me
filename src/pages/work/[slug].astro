---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

export async function getStaticPaths() {
  const posts = await getCollection("work");

  const publishedPosts = posts.filter(post => post.data.published !== false);

  return publishedPosts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
const heroImage = post.data.featuredImage ?? post.data.image;

const statusLabels: Record<string, string> = {
  "complete": "Shipped",
  "ongoing": "In Use",
  "in-progress": "Active",
  "backlog": "Planned",
};
---

<BaseLayout title={`${post.data.title} — Work`} description={post.data.description}>
  <article class="work-detail">
    <a href="/work" class="back-link">
      <svg viewBox="0 0 24 24"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
      Back to Work
    </a>

    {heroImage && (
      <figure class="article-hero">
        <img
          src={heroImage}
          alt={post.data.title}
          class="article-hero-image"
          loading="eager"
          decoding="async"
          fetchpriority="high"
        />
      </figure>
    )}

    <header class="article-header">
      <div class="article-meta">
        <span class="article-date">{post.data.date}</span>
        <span class="article-meta-divider">·</span>
        <span class="article-status">{statusLabels[post.data.status] ?? post.data.status}</span>
      </div>

      <h1 class="article-title">{post.data.title}</h1>
      <p class="article-description">{post.data.description}</p>

      <div class="article-tags">
        {post.data.tags.map((tag) => (
          <span class="article-tag">{tag}</span>
        ))}
      </div>

      {(post.data.repoUrl || post.data.liveUrl) && (
        <div class="article-links">
          {post.data.repoUrl && (
            <a href={post.data.repoUrl} class="article-link" target="_blank" rel="noopener noreferrer">
              <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              View Repository
            </a>
          )}
          {post.data.liveUrl && (
            <a href={post.data.liveUrl} class="article-link" target="_blank" rel="noopener noreferrer">
              <svg viewBox="0 0 24 24"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6M15 3h6v6M10 14L21 3"/></svg>
              Live Demo
            </a>
          )}
        </div>
      )}
    </header>

    <div class="article-content prose">
      <Content />
    </div>
  </article>
</BaseLayout>

<style>
  .work-detail {
    padding: var(--space-10) var(--space-7);
    max-width: 800px;
    margin: 0 auto;
  }

  .article-header {
    margin-bottom: var(--space-8);
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--ink-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: var(--space-6);
    transition: color 0.2s ease;
  }

  .back-link:hover {
    color: var(--accent);
  }

  .back-link svg {
    width: 18px;
    height: 18px;
    stroke: currentColor;
    stroke-width: 2;
    fill: none;
  }

  .article-meta {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--ink-muted);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    margin-bottom: var(--space-5);
  }

  .article-date {
    color: var(--ink-muted);
  }

  .article-meta-divider {
    opacity: 0.5;
  }

  .article-status {
    color: var(--accent);
  }

  .article-title {
    font-family: var(--font-display);
    font-size: var(--text-4xl);
    font-weight: 400;
    color: var(--ink);
    line-height: 1.15;
    letter-spacing: -0.02em;
    margin-bottom: var(--space-5);
  }

  .article-description {
    font-size: var(--text-lg);
    color: var(--ink-soft);
    line-height: 1.7;
    margin-bottom: var(--space-6);
    max-width: 60ch;
  }

  .article-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin-bottom: var(--space-6);
  }

  .article-tag {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--ink-muted);
    background: transparent;
    padding: 4px 10px;
    border: 1px solid var(--paper-edge);
    border-radius: 4px;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .article-links {
    display: flex;
    gap: var(--space-3);
    flex-wrap: wrap;
  }

  .article-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--ink-soft);
    padding: var(--space-3) var(--space-5);
    border: 1px solid var(--paper-edge);
    border-radius: 6px;
    transition: all 0.2s ease;
  }

  .article-link:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .article-link svg {
    width: 16px;
    height: 16px;
    stroke: currentColor;
    stroke-width: 1.5;
    fill: none;
  }

  /* Hero image — sits flat in the page, no shadow */
  .article-hero {
    margin: 0 0 var(--space-7) 0;
    padding: 0;
  }

  .article-hero-image {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 4px;
  }

  /* Prose styles for markdown content */
  .article-content {
    margin-top: var(--space-8);
    padding-top: var(--space-7);
    border-top: 1px solid var(--paper-edge);
  }

  .prose {
    font-size: var(--text-base);
    line-height: 1.8;
    color: var(--ink-soft);
  }

  .prose :global(h2) {
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    font-weight: 500;
    color: var(--ink);
    margin-top: var(--space-8);
    margin-bottom: var(--space-4);
    letter-spacing: -0.01em;
  }

  .prose :global(h3) {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 500;
    color: var(--ink);
    margin-top: var(--space-6);
    margin-bottom: var(--space-3);
  }

  .prose :global(p) {
    margin-bottom: var(--space-5);
  }

  .prose :global(ul),
  .prose :global(ol) {
    margin-bottom: var(--space-5);
    padding-left: var(--space-6);
  }

  .prose :global(li) {
    margin-bottom: var(--space-2);
  }

  .prose :global(blockquote) {
    border-left: 3px solid var(--accent);
    padding-left: var(--space-5);
    margin: var(--space-6) 0;
    font-style: italic;
    color: var(--ink);
  }

  .prose :global(code) {
    font-family: var(--font-mono);
    font-size: 0.9em;
    background: var(--cream-dark);
    padding: 2px 6px;
    border-radius: 4px;
  }

  .prose :global(pre) {
    background: var(--ink);
    color: var(--cream);
    padding: var(--space-5);
    border-radius: 8px;
    overflow-x: auto;
    margin: var(--space-6) 0;
  }

  .prose :global(pre code) {
    background: none;
    padding: 0;
    color: inherit;
  }

  .prose :global(a) {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .prose :global(a:hover) {
    color: var(--accent-light);
  }

  .prose :global(strong) {
    color: var(--ink);
    font-weight: 600;
  }

  .prose :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: var(--space-6) 0;
  }

  .prose :global(th),
  .prose :global(td) {
    padding: var(--space-3) var(--space-4);
    border: 1px solid var(--paper-edge);
    text-align: left;
  }

  .prose :global(th) {
    background: var(--cream-dark);
    font-weight: 600;
    color: var(--ink);
  }

  .prose :global(img) {
    width: 100%;
    max-width: 100%;
    height: auto;
    margin: var(--space-6) 0;
    border-radius: 8px;
  }

  @media (max-width: 768px) {
    .work-detail {
      padding: var(--space-9) var(--space-5);
    }

    .article-title {
      font-size: var(--text-3xl);
    }

    .article-links {
      flex-direction: column;
    }
  }
</style>
